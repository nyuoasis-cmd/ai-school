# 📊 스마트 전력 모니터링 — 데이터 & 시뮬레이션 스펙

## 1. 구역 데이터 (zones.js)

```javascript
export const zones = [
  {
    id: "computer",
    name: "3층 컴퓨터실",
    icon: "💻",
    bgColor: "rgba(0,212,255,0.15)",     // cyan-dim
    color: "#00D4FF",                     // cyan
    basePower: 16.1,  // kW
    pct: 34,
    status: "높음",
    statusColor: "#FF5252",               // red
  },
  {
    id: "lab",
    name: "2층 전기실습실",
    icon: "🔧",
    bgColor: "rgba(255,145,0,0.15)",      // orange-dim
    color: "#FF9100",                     // orange
    basePower: 10.8,
    pct: 23,
    status: "보통",
    statusColor: "#FFD600",               // yellow
  },
  {
    id: "classroom",
    name: "1층 교실동",
    icon: "📚",
    bgColor: "rgba(68,138,255,0.15)",
    color: "#448AFF",                     // blue
    basePower: 8.5,
    pct: 18,
    status: "보통",
    statusColor: "#FFD600",
  },
  {
    id: "gym",
    name: "체육관",
    icon: "🏀",
    bgColor: "rgba(0,230,118,0.15)",      // green-dim
    color: "#00E676",                     // green
    basePower: 5.2,
    pct: 11,
    status: "낮음",
    statusColor: "#00E676",
  },
  {
    id: "cafeteria",
    name: "급식실",
    icon: "🍚",
    bgColor: "rgba(255,214,0,0.15)",      // yellow-dim
    color: "#FFD600",                     // yellow
    basePower: 4.1,
    pct: 9,
    status: "낮음",
    statusColor: "#00E676",
  },
  {
    id: "admin",
    name: "행정동",
    icon: "🏢",
    bgColor: "rgba(179,136,255,0.15)",
    color: "#B388FF",                     // purple
    basePower: 2.5,
    pct: 5,
    status: "낮음",
    statusColor: "#00E676",
  },
];
```

---

## 2. 시간대별 데이터 (hourlyData.js)

```javascript
export const hourlyData = [
  { hour: "06", value: 12 },
  { hour: "07", value: 28 },
  { hour: "08", value: 52 },
  { hour: "09", value: 58 },
  { hour: "10", value: 65 },
  { hour: "11", value: 62 },
  { hour: "12", value: 35 },  // 점심시간 감소
  { hour: "13", value: 55 },
  { hour: "14", value: 61 },
  { hour: "15", value: 48 },
  { hour: "16", value: 38 },
  { hour: "17", value: 18 },
];

// 바 색상 분류 기준
export function getBarClass(value) {
  if (value >= 60) return "peak";   // red
  if (value >= 45) return "high";   // orange
  if (value >= 30) return "mid";    // cyan
  return "low";                      // green
}

// 최대값 (게이지 기준)
export const MAX_POWER = 70;       // kW
export const CONTRACT_POWER = 100; // kW
```

---

## 3. 알림 데이터 (alerts.js)

```javascript
export const alerts = [
  {
    type: "danger",       // red 좌측 보더
    icon: "🚨",
    title: "순간 최대전력 초과 경고",
    desc: '3층 컴퓨터실에서 순간 전력이 <b style="color:#FF5252">68.5kW</b>까지 상승했습니다. 계약전력의 97% 수준입니다.',
    time: "오늘 10:32",
  },
  {
    type: "warn",         // yellow 좌측 보더
    icon: "⚠️",
    title: "역률 저하 감지",
    desc: '2층 전기실습실 역률이 <b style="color:#FFD600">91.2%</b>로 하락했습니다. 콘덴서 점검을 권장합니다.',
    time: "오늘 09:15",
  },
  {
    type: "info",         // cyan 좌측 보더
    icon: "💡",
    title: "점심시간 자동 절전 실행",
    desc: "12:00 ~ 13:00 실습실 조명 자동 OFF 스케줄이 실행되었습니다. 예상 절감: 2.3kWh",
    time: "오늘 12:00",
  },
  {
    type: "info",
    icon: "📊",
    title: "일일 리포트 생성 완료",
    desc: '어제(2/5) 총 사용량 <b style="color:#00D4FF">328kWh</b>, 전일 대비 5.2% 감소. 우수 절전 구역: 1층 교실동',
    time: "오늘 08:00",
  },
  {
    type: "warn",
    icon: "🌡️",
    title: "변압기 온도 상승",
    desc: '지하 변전실 변압기 온도가 <b style="color:#FFD600">72°C</b>까지 상승. 정상 범위이나 모니터링 중.',
    time: "어제 16:40",
  },
  {
    type: "info",
    icon: "✅",
    title: "월간 절전 목표 달성 중",
    desc: '이번 달 목표 대비 <b style="color:#00E676">92%</b> 진행률. 현재 추세면 목표 달성 예상!',
    time: "어제 08:00",
  },
];

// 보더 색상 매핑
export const alertBorderColors = {
  danger: "#FF5252",
  warn: "#FFD600",
  info: "#00D4FF",
};
```

---

## 4. 실시간 시뮬레이션 로직 (useSimulation.js)

```javascript
import { useState, useEffect } from 'react';

// 기준값 ± 범위 내에서 랜덤 변동
function flutter(base, range, decimals = 1) {
  const val = base + (Math.random() - 0.5) * range;
  return Number(val.toFixed(decimals));
}

export function useSimulation() {
  const [liveData, setLiveData] = useState({
    power: 47.2,    // kW (현재 전력)
    voltage: 221,   // V
    current: 214,   // A
    frequency: 60.0, // Hz
    powerFactor: 96.5, // %
  });

  useEffect(() => {
    const interval = setInterval(() => {
      setLiveData({
        power: flutter(47.2, 4),        // 45.2 ~ 49.2
        voltage: Math.round(flutter(221, 4, 0)),  // 219 ~ 223
        current: Math.round(flutter(214, 10, 0)),  // 209 ~ 219
        frequency: flutter(60.0, 0.2),   // 59.9 ~ 60.1
        powerFactor: flutter(96.5, 1.5), // 95.75 ~ 97.25
      });
    }, 2000);

    return () => clearInterval(interval);
  }, []);

  // 게이지 퍼센트 계산 (최대 70kW 기준)
  const gaugePct = Math.round((liveData.power / 70) * 100);

  // 게이지 상태 판별
  let gaugeStatus, gaugeColor, gaugeBadge;
  if (gaugePct >= 90) {
    gaugeStatus = "danger";
    gaugeColor = "#FF5252";
    gaugeBadge = { text: "🚨 주의 필요", bg: "rgba(255,82,82,0.15)", color: "#FF5252" };
  } else if (gaugePct >= 75) {
    gaugeStatus = "warn";
    gaugeColor = "#FFD600";
    gaugeBadge = { text: "⚠️ 사용량 높음", bg: "rgba(255,214,0,0.15)", color: "#FFD600" };
  } else {
    gaugeStatus = "good";
    gaugeColor = "#00D4FF";
    gaugeBadge = { text: "✅ 정상 운영 중", bg: "rgba(0,230,118,0.15)", color: "#00E676" };
  }

  // SVG 게이지 stroke-dashoffset 계산
  const arcLength = 251; // 반원 둘레
  const dashOffset = arcLength - (arcLength * gaugePct / 100);

  return {
    liveData,
    gaugePct,
    gaugeStatus,
    gaugeColor,
    gaugeBadge,
    dashOffset,
  };
}
```

---

## 5. 요약 카드 데이터

```javascript
export const summaryCards = [
  {
    label: "현재 전력",
    // value는 liveData.power에서 가져옴 (실시간)
    unit: "kW",
    color: "#00D4FF",      // cyan
    change: "▼ 3.1%",
    changeType: "down",    // green
    changeLabel: "어제대비",
  },
  {
    label: "오늘 누적",
    value: 312,
    unit: "kWh",
    color: "#448AFF",      // blue
    change: "▼ 5.2%",
    changeType: "down",
    changeLabel: "어제대비",
  },
  {
    label: "이번달 요금",
    value: 847,
    unit: "만원",
    color: "#FFD600",      // yellow
    change: "▲ 2.3%",
    changeType: "up",
    changeLabel: "전월대비",
  },
];
```

---

## 6. 상태 카드 데이터

```javascript
export const statusCards = [
  {
    label: "전압",
    // value는 liveData.voltage (실시간)
    unit: "V",
    color: "#00D4FF",
    dotColor: "#00D4FF",
    sub: "정상 범위 220±10V",
    liveKey: "voltage",
  },
  {
    label: "전류",
    unit: "A",
    color: "#00E676",
    dotColor: "#00E676",
    sub: "정격 300A",
    liveKey: "current",
  },
  {
    label: "주파수",
    unit: "Hz",
    color: "#B388FF",
    dotColor: "#B388FF",
    sub: "정상 범위 59.9~60.1",
    liveKey: "frequency",
  },
  {
    label: "역률",
    unit: "%",
    color: "#FFD600",
    dotColor: "#FFD600",
    sub: "목표 95% 이상",
    liveKey: "powerFactor",
  },
];
```

---

## 7. AI 절전 분석 텍스트

```javascript
export const aiTip = {
  icon: "🤖",
  label: "AI 절전 분석",
  html: '현재 <b>3층 컴퓨터실</b>이 전체 사용량의 <b>34%</b>를 차지하고 있어요. 점심시간(12:00~13:00)에 실습실 조명과 모니터를 꺼두면 약 <b>월 12만원</b> 절감이 가능합니다.',
};

export const gradeInfo = {
  icon: "💡",
  label: "절전 등급",
  grade: "🅱",
  gradeText: "양호 등급",
  gradeColor: "#00E676",
  desc: '전월 대비 5.2% 절감 중!<br>A등급까지 <b style="color:#00D4FF">8%</b> 추가 절감 필요',
};
```

---

## 8. SVG 게이지 구현 가이드

### 반원 게이지 SVG 구조

```jsx
<svg viewBox="0 0 200 120" width="200" height="120">
  {/* 배경 아크 */}
  <path
    d="M 20 110 A 80 80 0 0 1 180 110"
    fill="none"
    stroke="#243056"
    strokeWidth={10}
  />
  {/* 채움 아크 */}
  <path
    d="M 20 110 A 80 80 0 0 1 180 110"
    fill="none"
    stroke={gaugeColor}
    strokeWidth={10}
    strokeLinecap="round"
    strokeDasharray={251}
    strokeDashoffset={dashOffset}
    style={{
      transition: 'stroke-dashoffset 1s ease, stroke 0.5s',
      filter: `drop-shadow(0 0 6px ${gaugeColor}40)`,
    }}
  />
</svg>
```

### 핵심 수식
- 반원 둘레 ≈ 251 (반지름 80 × π)
- `stroke-dashoffset = 251 - (251 × 퍼센트 / 100)`
- offset 0 = 100% 채움, offset 251 = 0% 채움

---

## 9. 컴포넌트 구조 (권장)

```
/src
  /components
    Header.jsx
    TabBar.jsx
    /dashboard
      SummaryRow.jsx       # 요약 카드 3종
      PowerGauge.jsx       # SVG 반원 게이지
      HourlyChart.jsx      # 시간대별 바 차트
      StatusGrid.jsx       # 전압/전류/주파수/역률
      AiTipCard.jsx        # AI 절전 분석
    /zones
      ZoneList.jsx         # 구역 카드 리스트
      ZoneBarChart.jsx     # 구역별 비율 수평 바
      GradeCard.jsx        # 절전 등급
    /alerts
      AlertList.jsx        # 알림 카드 리스트
  /data
    zones.js
    hourlyData.js
    alerts.js
    summaryCards.js
    statusCards.js
  /hooks
    useSimulation.js       # 실시간 시뮬레이션
  App.jsx
  index.css
```

---

## 10. 구현 우선순위

| 순서 | 항목 | 상세 |
|------|------|------|
| 1 | 다크 테마 기초 | CSS 변수, 배경, 폰트 |
| 2 | 레이아웃 | 헤더 + 탭바 + 콘텐츠 영역 |
| 3 | 대시보드 - 요약 | 3열 요약 카드 |
| 4 | 대시보드 - 게이지 | SVG 반원 게이지 |
| 5 | 대시보드 - 바차트 | 시간대별 12개 바 |
| 6 | 대시보드 - 상태 | 2×2 전압/전류/주파수/역률 |
| 7 | 대시보드 - AI팁 | 절전 분석 카드 |
| 8 | 구역별 탭 | 구역 리스트 + 비율 바 + 등급 |
| 9 | 알림 탭 | 알림 카드 6개 |
| 10 | 시뮬레이션 | setInterval 2초, 게이지 연동 |
| 11 | 폰 프레임 | 데스크탑 390×844 |
| 12 | 애니메이션 | fadeUp, blink, transition |
